// Student Controller — NIIST Academia

const getDashboard = async (req, res) => {
  res.json({ message: 'TODO: getDashboard' });
};

const getProfile = async (req, res) => {
  res.json({ message: 'TODO: getProfile' });
};

module.exports = { getDashboard, getProfile };
