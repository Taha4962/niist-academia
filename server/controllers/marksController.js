// Marks Controller — NIIST Academia

const getMarks = async (req, res) => {
  res.json({ message: 'TODO: getMarks' });
};

const addMarks = async (req, res) => {
  res.json({ message: 'TODO: addMarks' });
};

const updateMarks = async (req, res) => {
  res.json({ message: 'TODO: updateMarks' });
};

const getStudentMarks = async (req, res) => {
  res.json({ message: 'TODO: getStudentMarks' });
};

module.exports = { getMarks, addMarks, updateMarks, getStudentMarks };
