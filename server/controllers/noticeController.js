// Notice Controller — NIIST Academia

const getNotices = async (req, res) => {
  res.json({ message: 'TODO: getNotices' });
};

const createNotice = async (req, res) => {
  res.json({ message: 'TODO: createNotice' });
};

const markAsRead = async (req, res) => {
  res.json({ message: 'TODO: markAsRead' });
};

const deleteNotice = async (req, res) => {
  res.json({ message: 'TODO: deleteNotice' });
};

module.exports = { getNotices, createNotice, markAsRead, deleteNotice };
