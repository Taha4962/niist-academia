// Faculty Controller — NIIST Academia

const getDashboard = async (req, res) => {
  res.json({ message: 'TODO: getDashboard' });
};

const getMySubjects = async (req, res) => {
  res.json({ message: 'TODO: getMySubjects' });
};

const getMyStudents = async (req, res) => {
  res.json({ message: 'TODO: getMyStudents' });
};

module.exports = { getDashboard, getMySubjects, getMyStudents };
